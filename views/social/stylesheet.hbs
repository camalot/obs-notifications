{{#each networks}}
.{{name}}-pop {
background: {{background2}};
}
.{{name}}-pop .username span {
color: {{color}};
}
.popup .left .{{name}}:before {
background: {{background}} url('{{image}}') no-repeat 10px 10px;
}
{{/each}}

/* custom css */
{{custom_css.value}}
/* custom css end */
